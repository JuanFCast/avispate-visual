node_modules/
.next/
out/
next-env.d.ts
*.tsbuildinfo
.DS_Store

.vercel
.env*
!.env.example
docs/~$*
*.local.sql
# Documentación fuera de git POR AHORA (2026-08-16). La carpeta se reorganizó a
# mano (todo pasó a docs/Archivo/) y de momento se lleva en local. Los archivos
# siguen en disco y el historial conserva lo que había hasta este commit; lo que
# se pierde es el versionado de los cambios NUEVOS. Si se vuelve a rastrear,
# quitar esta línea y dejar las reglas concretas de abajo, que siguen valiendo.
docs/

# Archivos privados de pendientes y notas de sesión
docs/Pendientes de Avíspate.txt
docs/Pendientes de Avíspate.md
docs/Notas-sesion-*.md
docs/Guia-uso-Avispate-SEO-tecnico.docx

# Cableado local de las skills: son symlinks con ruta absoluta a este equipo.
# La skill de verdad vive en .agents/ y se reinstala con skills-lock.json.
.claude/